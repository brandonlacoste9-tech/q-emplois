# Neon — Projet "red ocean"
# Récupère l'URL dans: Neon Dashboard → Connection details → Connection string
DATABASE_URL=postgresql://[user]:[password]@[host]/neondb?sslmode=require

# Backend API
PORT=3001

# Frontend (optionnel — défaut: http://localhost:3001)
VITE_API_URL=http://localhost:3001

# WhatsApp Business — URL pour le bouton « Envoyer un message à Max »
# Ex: https://wa.me/15551234567
VITE_WHATSAPP_URL=https://wa.me/

# RBQ Verifier (tools/rbq_verifier) — pour POST /api/verify/rbq
# BROWSER_USE_API_KEY=   # cloud.browser-use.com
# LLM_PROVIDER=browser_use | openai
# RBQ_CACHE_TTL_HOURS=24

# Skill OpenClaw rbq_verifier (skills/rbq_verifier)
# RBQ_API_URL=http://localhost:3001/api/verify/rbq

# Metrics — Sécuriser /metrics et /metrics/dashboard en prod
# METRICS_API_KEY=       # X-API-Key ou ?api_key=

# Log Shipper — Axiom ou Datadog (activer au moins un)
# AXIOM_TOKEN=
# AXIOM_DATASET=qemplois-rbq-logs
# AXIOM_DOMAIN=api.axiom.co
# DD_API_KEY=
# DD_SITE=datadoghq.com
# DD_SERVICE=q-emplois-backend
# DD_ENV=production
