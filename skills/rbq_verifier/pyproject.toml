[project]
name = "rbq-verifier-skill"
version = "1.0.0"
description = "OpenClaw skill — Vérification licence RBQ"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.25.0",
    "pyyaml>=6.0",
]
